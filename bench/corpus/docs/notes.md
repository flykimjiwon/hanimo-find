# Operational notes

When a release misbehaves, undo it by shipping the previous build again.
Traffic is shifted to the new version in gradual increments to limit blast radius.
Approvals are required before a change reaches production users.
